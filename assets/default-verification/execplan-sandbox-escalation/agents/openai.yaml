interface:
  display_name: "Sandbox Escalation"
  short_description: "Handle out-of-sandbox command escalation safely"
  default_prompt: "Apply allowlist-first sandbox escalation workflow for ExecPlan tasks."

policy:
  allow_implicit_invocation: true
