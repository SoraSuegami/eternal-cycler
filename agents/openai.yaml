interface:
  display_name: "PR Auto Loop"
  short_description: "Start/continue fixed builder-reviewer loop"
  default_prompt: "Run the pr-autoloop skill workflow and invoke the non-interactive loop script with explicit task input."

policy:
  allow_implicit_invocation: true
