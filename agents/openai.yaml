interface:
  display_name: "Eternal Cycler: Autoloop — Automating the Builder–Reviewer Agent Interaction Cycle"
  short_description: "Start/continue fixed builder-reviewer loop"
  default_prompt: "Run the eternal-cycler skill workflow and invoke the non-interactive loop script with explicit task input."

policy:
  allow_implicit_invocation: true
